1. 공지사항 변수 체크 
    pageIndex=1&sorting=BOARD_ID&sortingAd=DESC&startDt=&endDt=&searchType=all&keyword=aa
    위의 변수값에 <>"'()/를 넣었을 때, alert("XSS")를 위한 "가 필터링이 되어있어 할 수 없다고 판단했다.
2. 작성된 글의 인덱스 값 변경
    board_id=17
    위의 변수값에 <>"'()/를 넣었을 때, <>의 문자만 필터링이 된다는 것을 확인했다.
    현재 인덱스값이 스크립트 내에 있었다. 
        function fnEdit(_board_id){
            document.location.href = 'noticeedit.php?board_id=' + 17;
        }
    2-1. 위에서 alert("XSS")를 입력하기 위해 형식을 맞춰주면, board_id=17;alert("XSS")와 같은 형태가 된다.
    하지만 다른 링크로 넘어가는 함수이기 때문에 동작하지 않았다.
    2-2. 함수를 빠져 나와도 스크립트는 동작한다. 그래서 함수를 닫아주고, alert를 해준 후 ;}를 맞춰주기 위해
         새로운 함수를 추가하였다.
        최종 url: https://lab.eqst.co.kr:8083/exam17/noticeview.php
        ?board_id=17;}alert("XSS");+function+aaa(){console.log('hihi')

[답] xss_exam